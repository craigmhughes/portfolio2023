import type {NextPage} from 'next';

import Layout from '@/components/Layout';

const Personal: NextPage = (): JSX.Element => (
    <Layout>
        <main>
            <h1>Hi there</h1>
        </main>
    </Layout>
);

export default Personal;
